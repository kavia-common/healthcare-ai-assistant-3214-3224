# Application
ENVIRONMENT=development

# Database
# Default to SQLite local file for development.
# For a different location, set an absolute or relative path.
DATABASE_URL=sqlite:///./dev.db

# OpenAI (optional for development; mock responses used if unset)
OPENAI_API_KEY=

# CORS
# Comma-separated origins (e.g., http://localhost:3000)
CORS_ALLOW_ORIGINS=*
